version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
    # volumes:
    #   - ./redis-data:/data
    # command: ["redis-server", "--appendonly", "yes"]
    restart: always 

  server:
    image: glad47/sales-server:07
    container_name: server
    ports:
      - "8888:8888"
    restart: always 

  app:
    image: glad47/sales-system:013
    container_name: app
    ports:
      - "80:80"
    restart: always  
